checkout:
  post:
    - sudo apt-get -y install cpanminus
    - sudo cpanm rlib Module::Build Text::Diff Test::Simple Data::Printer Test::Pod

test:
  override:
    - perl ./Build.PL
    - perl ./Build installdeps
    - GIT2CL=./git2cl make check ChangeLog distmeta distcheck
