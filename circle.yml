checkout:
  post:
    - sudo apt-get -y install cpanminus
    - sudo cpanm rlib Module::Build Text::Diff Test::Simple

test:
  override:
    - perl ./Build.PL
    - perl ./Build installdeps
    - make check ChangeLog distmeta distcheck
